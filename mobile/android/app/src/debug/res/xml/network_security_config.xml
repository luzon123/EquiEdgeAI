<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG BUILD ONLY (app/src/debug/... is merged only into debug builds by
  the Android Gradle Plugin — the release build has no equivalent file and
  stays fully HTTPS-only/cleartext-blocked, as it should be).

  Since Android 9 (API 28), apps cannot make plain HTTP (cleartext)
  connections by default when targetSdkVersion >= 28 — this project targets
  34, so http://127.0.0.1:5000 (the debug API_BASE_URL in app/build.gradle,
  used with `adb reverse tcp:5000 tcp:5000` for physical-device dev) is
  blocked unless explicitly allowed here. This is scoped to only the fixed
  local-development loopback addresses actually used by the debug build
  (127.0.0.1 for adb reverse, 10.0.2.2 for the emulator) — not a blanket
  cleartext allowance for the whole app or the whole internet.

  Testing over Wi-Fi/LAN instead of adb reverse: add your computer's LAN IP
  as another <domain> entry here (e.g. <domain
  includeSubdomains="false">192.168.1.23</domain>) and point
  API_BASE_URL at it in app/build.gradle.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
