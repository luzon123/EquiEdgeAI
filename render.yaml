databases:
  # Render-managed PostgreSQL — persistent across all redeployments.
  # Free tier is sufficient for launch; upgrade in the Render dashboard when needed.
  - name: equiedge-db
    databaseName: equiedge
    plan: free

services:
  - type: web
    name: equiedge-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "app:create_app()" --workers 2 --threads 2 --bind 0.0.0.0:$PORT --timeout 120
    healthCheckPath: /health

    envVars:
      # ── Core (required) ─────────────────────────────────────────────────
      - key: SECRET_KEY
        sync: false          # Set this in the Render dashboard — never commit the value

      # ── Database ─────────────────────────────────────────────────────────
      # Automatically populated from the equiedge-db PostgreSQL service above.
      # Do NOT set this manually in the dashboard — fromDatabase overrides it.
      - key: DATABASE_URL
        fromDatabase:
          name: equiedge-db
          property: connectionString

      # ── PayPal (all required) ────────────────────────────────────────────
      - key: PAYPAL_MODE
        value: live          # Change to 'sandbox' for staging deployments
      - key: PAYPAL_CLIENT_ID
        sync: false
      - key: PAYPAL_CLIENT_SECRET
        sync: false
      - key: PAYPAL_WEBHOOK_ID
        sync: false          # Required when PAYPAL_MODE=live

      # ── Paddle (card checkout — optional, button hidden when absent) ──
      - key: PADDLE_ENV
        value: sandbox       # Change to 'production' when your Paddle account is approved
      - key: PADDLE_CLIENT_TOKEN
        sync: false          # From Paddle dashboard → Developer → Authentication
      - key: PADDLE_API_KEY
        sync: false          # From Paddle dashboard → Developer → Authentication
      - key: PADDLE_WEBHOOK_SECRET
        sync: false          # From Paddle dashboard → Developer → Notifications → webhook secret
      - key: PADDLE_BEGINNER_PRICE_ID
        sync: false          # From Paddle dashboard → Catalog → Prices
      - key: PADDLE_PRO_PRICE_ID
        sync: false          # From Paddle dashboard → Catalog → Prices

      # ── Email / SMTP ─────────────────────────────────────────────────────
      - key: MAIL_SERVER
        sync: false          # e.g. smtp.mailgun.org
      - key: MAIL_PORT
        value: "587"
      - key: MAIL_USE_TLS
        value: "true"
      - key: MAIL_USERNAME
        sync: false
      - key: MAIL_PASSWORD
        sync: false
      - key: MAIL_DEFAULT_SENDER
        value: noreply@equiedgeai.io
      - key: CONTACT_RECIPIENT
        sync: false          # Email address that receives contact form submissions
